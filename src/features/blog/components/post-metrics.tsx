/**
 * Stub PostMetrics — Task 6 thay bằng client island (views + like).
 *
 * Giữ export và signature `({ slug })` để trang bài viết compile trước Task 6.
 */
export function PostMetrics({ slug: _slug }: { slug: string }) {
  return null;
}
